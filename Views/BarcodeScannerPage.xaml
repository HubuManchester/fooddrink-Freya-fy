<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.Maui.Controls"
    x:Class="NutriLens.Views.BarcodeScannerPage"
    Title="Scan Barcode"
    BackgroundColor="Black">

	<Grid>
		<zxing:CameraBarcodeReaderView
            x:Name="BarcodeReader"
            IsDetecting="True" />

		<Grid InputTransparent="True">
			<Frame
                Margin="50,120,50,220"
                BorderColor="#4CAF50"
                BackgroundColor="Transparent"
                CornerRadius="16"
                HasShadow="False"
                HeightRequest="240"
                WidthRequest="280"
                HorizontalOptions="Center"
                VerticalOptions="Center" />

			<Label
                Text="Align barcode within the green frame"
                TextColor="#E0E0E0"
                FontSize="14"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                Margin="0,280,0,0" />
		</Grid>

		<Grid VerticalOptions="End"
              Padding="24,0,24,40"
              ColumnDefinitions="*,*"
              ColumnSpacing="16">

			<Button
                Grid.Column="0"
                Text="Cancel"
                BackgroundColor="#80000000"
                TextColor="White"
                BorderColor="White"
                BorderWidth="1"
                CornerRadius="24"
                HeightRequest="54"
                FontSize="15"
                FontAttributes="Bold"
                Clicked="OnCancelClicked"
                SemanticProperties.Hint="Cancels barcode scanning and returns to the previous screen" />

			<Button
                x:Name="TorchButton"
                Grid.Column="1"
                Text="🔦 Torch On"
                BackgroundColor="#80000000"
                TextColor="White"
                BorderColor="White"
                BorderWidth="1"
                CornerRadius="24"
                HeightRequest="54"
                FontSize="15"
                FontAttributes="Bold"
                Clicked="OnTorchClicked"
                SemanticProperties.Hint="Toggles the device camera flashlight on or off" />
		</Grid>
	</Grid>
</ContentPage>